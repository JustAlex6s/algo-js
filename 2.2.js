let min = prompt(" Min ?");
let max = prompt(" Max ?");
let current = prompt(" Current ?");


if ((current > min) && (current < max)) {
    console.log("vrai" + current);
  } else {
    console.log("faux");
  }
